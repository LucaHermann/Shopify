<section id="section-{{ section.id }}" class="section" data-section-id="{{ section.id }}" data-section-type="[[ footer-info-custom ]]">
  <div class="fic__main-container-padding">
    <div class="fic__main-container">
      {% for block in section.blocks %}
      <div class="fic__block-container">
        <div class="fic__block-image">
          <img alt="fic__icon-footer-info" src="{{ block.settings.icon | img_url: '100x100' }}" >
        </div>
        <div class="h4 fic__block-heading">
          {{ block.settings.heading }} 
        </div>
        <div class="fic__block-info">
          {{ block.settings.first_info }} 
        </div>
        <div class="fic__block-info">
          {{ block.settings.second_info }} 
        </div>
      </div>
      {% endfor %}
    </div>
  </div>
</section>

{% schema %}
{
 	"name": "footer-info-custom",
 	"settings": [],
 	"blocks": [
	{
 		"name": "icon text section",
 		"type": "image",
 		"settings": [{
 				"id": "icon",
 				"type": "image_picker",
 				"label": "Icon info"
 			},
 			{
 				"id": "heading",
 				"type": "richtext",
 				"label": "heading info"
 			},
 			{
 				"id": "first_info",
 				"type": "richtext",
 				"label": "first info"
 			},
 			{
 				"id": "second_info",
 				"type": "richtext",
 				"label": "second info"
 			}
 		]
 	}
],
 	"presets": [{
 		"name": "footer-info-custom",
 		"category": "SECTIONS CUSTOM"
 	}]
}
{% endschema %}